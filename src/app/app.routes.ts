import { Routes } from '@angular/router';
import { PlovLandingComponent } from './landing/plov-landing.component';

export const routes: Routes = [
  {
    path: '',
    component: PlovLandingComponent
  }
];